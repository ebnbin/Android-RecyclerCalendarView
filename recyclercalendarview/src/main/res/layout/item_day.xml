<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/size_day">
    <FrameLayout
        android:id="@+id/day_wrapper"
        android:layout_width="@dimen/size_day"
        android:layout_height="@dimen/size_day"
        android:layout_gravity="center_horizontal">
        <TextView
            android:id="@+id/day"
            android:layout_width="@dimen/size_day_text"
            android:layout_height="@dimen/size_day_text"
            android:layout_gravity="center"
            android:gravity="center"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:textColor="@color/text_day"
            android:textSize="@dimen/text_day"/>
    </FrameLayout>
</FrameLayout>
